"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkConfigForCredentials = void 0;
const common_tags_1 = require("common-tags");
const logger_1 = require("../utils/logger");
function checkConfigForCredentials(config, shouldExit = true) {
    if (config.username && config.password) {
        return;
    }
    const msg = common_tags_1.stripIndent `
      We couldn't find any Twilio credentials for you.

      Please enter ACCOUNT_SID and AUTH_TOKEN in your .env file.

      Alternatively you can specify them via the command-line using "--username" and "--password"

      For more information use "--help"
    `;
    logger_1.logger.error(msg, 'Missing Credentials');
    if (shouldExit) {
        process.exit(1);
    }
}
exports.checkConfigForCredentials = checkConfigForCredentials;
