declare type Options = {
    shouldPrintMessage: boolean;
    shouldThrowError: boolean;
    functionName?: string;
};
export declare function checkForValidAccountSid(accountSid: string | undefined, options?: Options): boolean;
export {};
