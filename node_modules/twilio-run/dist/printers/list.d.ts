import { AssetVersion, FunctionVersion, ListResult } from '@twilio-labs/serverless-api';
import { OutputFormat } from '../commands/shared';
import { ListConfig } from '../config/list';
export declare type CommonKeysFunctionAndAssetVersion = Extract<keyof FunctionVersion, keyof AssetVersion>;
export declare type CommonType = {
    [key in CommonKeysFunctionAndAssetVersion]: FunctionVersion[key];
};
export declare type FunctionOrAssetContent = {
    environmentSid: string;
    entries: CommonType[];
};
export declare function printListResult(result: ListResult, config: ListConfig, outputFormat: OutputFormat): void;
