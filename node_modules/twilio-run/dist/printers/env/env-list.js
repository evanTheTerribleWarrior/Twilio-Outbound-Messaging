"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.outputVariables = void 0;
const chalk_1 = __importDefault(require("chalk"));
const output_1 = require("../../utils/output");
function outputVariables(result, format) {
    if (format === 'json') {
        output_1.writeJSONOutput(result);
    }
    else {
        const output = result.variables
            .map((entry) => {
            const key = chalk_1.default `{bold ${entry.key}}`;
            const value = typeof entry.value !== 'undefined'
                ? entry.value
                : chalk_1.default `{dim Use --show-values to display value}`;
            return `${key} ${value}`;
        })
            .join('\n');
        output_1.writeOutput(output);
    }
}
exports.outputVariables = outputVariables;
