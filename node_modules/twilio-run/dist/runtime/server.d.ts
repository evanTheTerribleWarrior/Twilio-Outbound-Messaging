import { Express } from 'express';
import { StartCliConfig } from '../config/start';
export declare function createLocalDevelopmentServer(port: string | number | undefined, config: StartCliConfig): Promise<Express>;
/** @deprecated */
export declare function createServer(port: string | number | undefined, config: StartCliConfig): Promise<Express>;
export declare function runServer(port: string | number | undefined, config: StartCliConfig): Promise<Express>;
