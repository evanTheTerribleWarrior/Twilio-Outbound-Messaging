import { ListConfig as ApiListConfig } from '@twilio-labs/serverless-api';
import { Arguments } from 'yargs';
import { ExternalCliOptions } from '../commands/shared';
import { AllAvailableFlagTypes, SharedFlagsWithCredentialNames } from '../flags';
export declare type ListConfig = ApiListConfig & {
    username: string;
    password: string;
    cwd: string;
    properties?: string[];
    extendedOutput: boolean;
    outputFormat?: string;
};
export declare type ConfigurableListCliFlags = Pick<AllAvailableFlagTypes, SharedFlagsWithCredentialNames | 'serviceName' | 'properties' | 'extendedOutput' | 'environment' | 'serviceSid' | 'outputFormat'>;
export declare type ListCliFlags = Arguments<ConfigurableListCliFlags & {
    types: string;
    projectName?: string;
}>;
export declare function getConfigFromFlags(flags: ListCliFlags, externalCliOptions?: ExternalCliOptions): Promise<ListConfig>;
