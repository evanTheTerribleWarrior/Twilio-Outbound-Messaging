import { SetEnvironmentVariablesConfig as ApiEnvironmentConfig } from '@twilio-labs/serverless-api';
import { Arguments } from 'yargs';
import { ExternalCliOptions } from '../../commands/shared';
import { AllAvailableFlagTypes, SharedFlagsWithCredentialNames } from '../../flags';
export declare type EnvSetConfig = ApiEnvironmentConfig & {
    username: string;
    password: string;
    cwd: string;
};
export declare type ConfigurableEnvGetCliFlags = Pick<AllAvailableFlagTypes, SharedFlagsWithCredentialNames | 'serviceSid' | 'environment' | 'production'>;
export declare type EnvSetFlags = Arguments<ConfigurableEnvGetCliFlags & {
    key: string;
    value: string;
}>;
export declare function getConfigFromFlags(flags: EnvSetFlags, externalCliOptions?: ExternalCliOptions): Promise<EnvSetConfig>;
