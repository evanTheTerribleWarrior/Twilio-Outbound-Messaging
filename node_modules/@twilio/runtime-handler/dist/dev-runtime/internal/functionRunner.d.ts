import { ServerConfig, Headers } from '../types';
export declare type Reply = {
    body?: string | number | boolean | object;
    headers?: Headers;
    statusCode: number;
};
export declare type FunctionRunnerOptions = {
    functionPath: string;
    event: {
        [key: string]: any;
    };
    config: ServerConfig;
    path: string;
};
