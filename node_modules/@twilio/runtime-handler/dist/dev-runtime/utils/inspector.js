"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.startInspector = void 0;
const inspector_1 = __importDefault(require("inspector"));
function startInspector(val, wait = false) {
    let port = undefined;
    let host = undefined;
    if (typeof val === 'string') {
        if (val.includes(':')) {
            const [hostRaw, portRaw] = val.split(':');
            port = parseInt(portRaw, 10);
            host = hostRaw;
        }
        else if (val.length > 0) {
            port = parseInt(val, 10);
        }
    }
    inspector_1.default.open(port, host, wait);
    return inspector_1.default;
}
exports.startInspector = startInspector;
