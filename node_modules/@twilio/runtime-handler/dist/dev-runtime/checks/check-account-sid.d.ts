import { LoggerInstance } from '../types';
declare type Options = {
    shouldPrintMessage: boolean;
    shouldThrowError: boolean;
    functionName?: string;
    logger?: LoggerInstance;
};
export declare function checkForValidAccountSid(accountSid: string | undefined, options?: Options): accountSid is string;
export {};
